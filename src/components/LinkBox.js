import { Button } from "../styles/Components/Button";
import { Ico } from "../styles/Components/Ico";

function ButtonBox(link) {
    console.log(link)
    return(
        console.log(link.name)
    )
};

export default ButtonBox;